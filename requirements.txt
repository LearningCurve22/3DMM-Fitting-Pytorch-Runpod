# requirements.txt
numpy
scipy
opencv-python
Pillow
matplotlib
tqdm
scikit-image
face_alignment
facenet-pytorch
fvcore
iopath
psutil
torchvision  # if torch is preinstalled in base image, this is okay but will not reinstall torch
# NOTE: do not include 'torch' here if you already DEFINED torch in the base image or installed explicitly in Dockerfile
# pytorch3d is installed via Dockerfile (git+...), but if you prefer pip wheel version, add it here matching torch version.
